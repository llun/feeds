image: node:14

pages:
  stage: deploy
  script:
    - npm install
    - npm run load
    - npm run build -- --output=public
  artifacts:
    paths:
      - public
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
