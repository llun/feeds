name: Schedule

on:
  schedule:
    - cron: '0,15,30,45 * * * *'

jobs:
  playground:
    runs-on: ubuntu-latest
    name: Test
    steps:
      - name: Run Action
        uses: llun/test-action@main
        with:
          customDomain: feeds.llun.dev
