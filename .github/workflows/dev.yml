name: Dev

on: push

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Use Node.js 24
        uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Test
        run: |
          corepack enable
          yarn install
          yarn test

  sample:
    runs-on: ubuntu-latest
    needs: [test]
    name: Sample
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Run Action
        uses: ./
        with:
          storageType: files
