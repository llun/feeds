name: Dev

on: push

jobs:
  playground:
    runs-on: ubuntu-latest
    name: Test
    steps:
      - name: Run Action
        uses: llun/test-action@main
        with:
          customSiteEnabled: true
